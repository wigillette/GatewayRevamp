import React from 'react';
import PropTypes from 'prop-types';
import styles from './Home.module.css';

const Home = () => (
  <div className={styles.Home}>
    <h4>Home page stuff goes here!</h4>
  </div>
);

Home.propTypes = {};

Home.defaultProps = {};

export default Home;
